# CLAUDE.md — makishima-kei 実験進行管理

## 基準日の取得（最重要）

**システムコンテキストの `currentDate` はUTC基準のため使用禁止。**
セッション開始時は必ず以下のBashコマンドで日本標準時（JST）の日付を取得し、それを基準日として使用すること。

```bash
TZ=Asia/Tokyo date +"%Y/%m/%d"
```

理由: このルーティンは深夜〜早朝（日本時間）に実行されることがあり、UTC日付は JST より1日前になる場合がある（UTC+9のため、00:00〜08:59 JST は前日UTC）。プロンプトに「基準日は実行日（Asia/Tokyo）」と明記されているとおり、必ずJSTで判断すること。
